<div class="text-center mt-10">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">Welcome to EmrHmsdev</h1>
  <p class="text-gray-600 text-lg mb-8">Your all-in-one healthcare management system</p>
  <% if user_signed_in? %>
    <p>Welcome, <%= current_user.email %></p>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-red-600 font-semibold" %>
  <% else %>
    <%= link_to 'Sign up', new_user_registration_path %> |
    <%= link_to 'Log in', new_user_session_path %>
  <% end %>
</div>
