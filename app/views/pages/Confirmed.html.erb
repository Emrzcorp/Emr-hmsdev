<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Confirmation - Chen Patient</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
<style>
  body {
    background: #fff;
    margin: 0;
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  }
  .modal-custom {
    max-width: 420px;
    border: 1px solid #003a75;
    border-radius: 0;
  }
  .modal-header-custom {
    background-color: #003a75;
    color: white;
    font-weight: 500;
    font-size: 14px;
    padding: 0.375rem 1rem;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .modal-header-custom .title {
    user-select: none;
  }
  .modal-header-custom .btn-close {
    filter: invert(1);
    opacity: 0.7;
    margin: 0;
    padding: 0;
    width: 20px;
    height: 20px;
  }
  .modal-body-custom {
    padding: 0.75rem 1rem 1rem 1rem;
    font-size: 13px;
    color: #000;
  }
  .form-label {
    font-weight: 600;
    font-size: 13px;
    margin-bottom: 0.25rem;
  }
  .form-check-label {
    font-weight: 400;
    font-size: 13px;
  }
  .form-select, .form-control {
    font-size: 13px;
    height: 30px;
    padding: 0 0.375rem;
  }
  .form-control {
    resize: none;
  }
  .form-control::placeholder {
    font-size: 13px;
  }
  .form-textarea-wrapper {
    margin-top: 0.5rem;
  }
  .modal-footer-custom {
    padding: 0.375rem 1rem;
    border-top: 1px solid #003a75;
    display: flex;
    justify-content: space-between;
    background: #fff;
  }
  .btn-cancel {
    font-size: 13px;
    font-weight: 400;
    color: #003a75;
    background: #fff;
    border: 1px solid #003a75;
    padding: 0.25rem 0.75rem;
    height: 30px;
    line-height: 1.2;
  }
  .btn-cancel:hover, .btn-cancel:focus {
    background: #e6f0ff;
    color: #003a75;
  }
  .btn-save {
    font-size: 13px;
    font-weight: 600;
    color: #fff;
    background: #003a75;
    border: 1px solid #003a75;
    padding: 0.25rem 0.75rem;
    height: 30px;
    line-height: 1.2;
  }
  .btn-save:hover, .btn-save:focus {
    background: #002a55;
    border-color: #002a55;
  }
  .required-star {
    color: #d00;
    margin-left: 2px;
    font-weight: 600;
  }
  .d-flex-gap {
    display: flex;
    gap: 0.75rem;
  }
  .d-flex-gap > div {
    flex: 1 1 0;
  }
</style>
</head>
<body>
<div class="d-flex justify-content-center align-items-center vh-100">
  <div class="modal-custom" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
    <div class="modal-header-custom">
      <div id="modalTitle" class="title">Confirmation - Chen Patient</div>
      <button type="button" class="btn-close" aria-label="Close"></button>
    </div>
    <div class="modal-body-custom">
      <form>
        <div class="mb-2">
          <label class="form-label">Appointment confirmation</label>
          <div class="form-check">
            <input class="form-check-input" type="radio" name="confirmation" id="notConfirmed" value="notConfirmed" />
            <label class="form-check-label" for="notConfirmed">Not confirmed</label>
          </div>
          <div class="form-check mb-2">
            <input class="form-check-input" type="radio" name="confirmation" id="confirmed" value="confirmed" checked />
            <label class="form-check-label" for="confirmed">Confirmed</label>
          </div>
        </div>
        <div class="d-flex-gap mb-2">
          <div>
            <label for="updatedBy" class="form-label">UPDATED BY <span class="required-star">*</span></label>
            <select id="updatedBy" class="form-select" aria-required="true" aria-describedby="updatedByHelp">
              <option>Julian Provider</option>
            </select>
          </div>
          <div>
            <label for="date" class="form-label">DATE <span class="required-star">*</span></label>
            <input type="date" id="date" class="form-control" value="2022-10-19" aria-required="true" />
          </div>
        </div>
        <div class="d-flex-gap mb-2">
          <div>
            <label for="confirmationMethod" class="form-label">CONFIRMATION METHOD <span class="required-star">*</span></label>
            <select id="confirmationMethod" class="form-select" aria-required="true">
              <option>Spoke with caregiver</option>
            </select>
          </div>
          <div>
            <label for="time" class="form-label">TIME <span class="required-star">*</span></label>
            <input type="time" id="time" class="form-control" value="11:02" aria-required="true" />
          </div>
        </div>
        <div class="form-textarea-wrapper">
          <label for="notes" class="form-label">NOTES</label>
          <textarea id="notes" class="form-control" rows="2" placeholder="Notes"></textarea>
        </div>
      </form>
    </div>
    <div class="modal-footer-custom">
      <button type="button" class="btn btn-cancel">Cancel</button>
      <button type="submit" class="btn btn-save">Save</button>
    </div>
  </div>
</div>
</body>
</html>